{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "configure",
            "command": "cmake .. && make",
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ],
}